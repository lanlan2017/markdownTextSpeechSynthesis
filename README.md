## 安装 ##
进入`runable`目录,点击以管理员身份运行`install.bat`即可安装.
这个批处理脚本会把生成程序入口`xunfei.bat`,然后把`xunfei.bat`所在的路径添加到`path`环境变量中.
## 运行 ##
按下`win+r`,然后输入`xunfei`即可运行.
## 卸载 ##
进入`runable`目录,点击以管理员身份运行`uninstall.bat`即可卸载.
运行`uninstall.bat`不会删除项目,只会把`xunfei.bat`所在的目录从`Path`环境变量中移除掉。